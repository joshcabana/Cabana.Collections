(function(){
  // CABANA launch flags (static defaults)
  // Keep checkout and auth disabled until flows are ready
  window.CABANA_FLAGS = {
    checkoutEnabled: false,
    authEnabled: false
  };
})();


